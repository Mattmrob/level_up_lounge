<!DOCTYPE html>
<html lang="en">

<head>
  <meta charset="UTF-8">
  <meta name="viewport" content="width=device-width, initial-scale=1.0">
  <title>LevelUpLounge</title>
  <link rel="stylesheet" href="/css/jass.css">
  <link rel="stylesheet" href="/css/style.css">
</head>

<body class="mainBackground">

  <div class="loginBlock">
    {{#if logged_in}}
      <a href="/profile"><h1>{{name}}</h1></a>
      <button id="logout">logout</button>
      <a href="/">Return Home</a>
    {{else}}
      <a href="/login"><h1 class="loginUserButton">Log In!</h1></a>
      <p class="loginMessage">You need to be logged in to post</p>
    {{/if}}
    </div>

  <div class="row-md-12 flex-column justify-space-apart">
    <div class="col-md-12">
  {{!-- THIS IS ALL THE NAV BAR --}}
      {{!-- <header class="display-flex justify-space-between align-center p-2">
        <h1>
          <span role="img" aria-label="money">💸</span>
          <a href="/">LevelUpLounge</a>
        </h1>
        <nav>
          {{#if logged_in}}
          <a href="/profile">profile</a> |
          <button class="no-button" id="logout">logout</button>
          {{else}}
          <a href="/login">login</a>
          {{/if}}
        </nav>
      </header> --}}
      {{!-- NAV BAR END --}}

      {{!-- THIS IS WHERE OUR CARDS ARE RENDERED --}}
      <main class="container container-fluid mt-5">
        {{{ body }}}
      </main>
      {{!-- RENDER END --}}
    </div>
  </div>

  <!-- Render script for logged in users only -->
  {{#if logged_in}}
  <script src="/js/logout.js"></script>
  {{/if}}
</body>

</html>
